{% extends 'base.html.twig' %}

{% block body %}
    <h1>Emplacement</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ emplacement.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ emplacement.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_domotique_emplacement_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('admin_domotique_emplacement_edit', { 'id': emplacement.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
